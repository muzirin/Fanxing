# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格，版本号语义见 `package.json`。

## [1.0.0-b1] - 2026-09-23

首个 Beta：核心「课程 / 作业 / 本地开发 / 提交」链路全通。

### Added

- **课程**：课程列表（TreeView），课程 → 章节 → 任务点懒加载（`mycourse/studentcourse` 章节树 + `knowledge/cards` 任务点），任务点完成状态与课程进度
- **作业**：按课程分组列表（待做/已完成/未开放/已截止全量保留，不隐藏），作业显示筛选（全部 ↔ 仅待完成），截止倒计时与临近预警
- **作业面板**：题型全覆盖（单选/多选/判断/填空/简答/程序设计），KaTeX 公式与公式图还原（含 `ans-formula-moudle` LaTeX 解码），题库编号、样例表格、限制展示，每题独立「测试 / 提交结果」区
- **编程作业本地开发**：每题独立源文件（`q1.c`/`q2.cpp`/`q1.py`；Java `Main.java` 按 `q{n}/` 目录区分）+ 按题独立用例目录；历史文件自动迁移
- **本地判题**：编译运行 + 样例 diff，逐用例状态/耗时；**始终展示控制台输出**，失败用例附标准错误、进程退出码与崩溃解读（`0xC0000005 访问违例`、SIGSEGV/SIGABRT/SIGFPE 等）
- **提交**：本地样例全过后提交（可配置），提交前逐题预览，逐题附带各自代码，轮询平台判题结果并记录历史
- **通知 / 签到 / 成绩 / 日程**：通知分类与桌面提醒、签到检测与本人确认签到（不做位置模拟、不代签）、成绩趋势、DDL 汇总与 `.ics` 导出
- **AI 引导**：代码审查 / 思路引导 / 错误分析 / 复杂度分析，OpenAI 兼容提供商，系统提示词层面禁止代写完整答案
- **环境检测**：gcc/g++ / JDK / Python 检测与分平台安装引导
- **账号与安全**：扫码/密码/短信/Cookie 登录，多账号切换，凭据全部 `SecretStorage`，日志自动脱敏
- **取证工具**：`Fanxing: 抓取作业原始响应` 全链路导出（课程中间页 / 作业列表 / stu-work / 通知 / 签到）到 `fanxing-debug/`
- 中文 / 英文界面（`fanxing.locale`）

### Fixed

- 作业列表走 `work/list` 签名链路（中间页 `workEnc`/`stuenc`/`t`），多变体兜底 + `stu-work` 全局列表兜底；移除已 404 的废弃端点
- 章节与进度取自 `mycourse/studentcourse`（`已完成任务点: N/M`）
- 题目解析适配真实批改页（`questionLi` 容器 / `.qtContent` 题干），修复伪题目块、题干混入作答代码、公式空白
- 样例按「样例N：」标记配对，不再把输入/输出说明误当样例
- 多题作业逐题独立测试与提交（此前共用一个文件互相污染）
- 本地测试进程提前退出时的 stdin EPIPE 流错误导致扩展宿主崩溃（闪退）；全面加固未处理 Promise 拒绝路径
- 切换作业自动保存并关闭当前作业；同一作业各题文件复用同一编辑器组

[1.0.0-b1]: https://github.com/muzirin/Fanxing/releases/tag/1.0.0-b1
